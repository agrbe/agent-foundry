interface:
  display_name: "Git Commit Message"
  short_description: "Draft, review, and rewrite commit messages"
  default_prompt: "Use $git-commit-message to draft or review a commit message from my staged changes, diff, or current repository conventions."
