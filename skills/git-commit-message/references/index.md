# References index

Use these files in the following order:

1. `commit-message-rules.md`
   Primary rulebook for message structure, style selection, trailers, and review criteria.

2. `repo-detection-checklist.md`
   Practical guide for determining whether the repository expects plain Git style, Conventional Commits, or a custom format.

3. `source-notes.md`
   Maintenance notes describing where the guidance came from and when it was last verified.

Use `examples/` for behavior anchoring and `templates/` for operational reuse.

